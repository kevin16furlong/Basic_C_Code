#include <stdio.h>
#include<math.h>


int main( int argc, char *argv[] )  {
   int aor;

   aor = areaOfRectangle(10,80);
   printf("Area of Recctangle: %d", aor);
}

// a program to find the area of different shapes, 
// using a makefile to compile a group of c and header files together. 